/* ========================================
   resources/index.js - Resources module entry
   ======================================== */

Router.register('resources', () => {
  ResourceView.init();
});

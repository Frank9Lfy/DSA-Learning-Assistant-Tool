# 🚀 快速开始指南

## 第一步：打开网站

### 方式1：直接双击打开（最简单）
```
1. 打开文件管理器
2. 进入 dsa-review 文件夹
3. 双击 index.html 文件
4. 网站将在浏览器中打开
```

### 方式2：使用 VS Code（推荐，体验更好）
```
1. 用 VS Code 打开 dsa-review 文件夹
2. 安装 "Live Server" 扩展
3. 右键 index.html → "Open with Live Server"
4. 网站将在浏览器中打开，且支持热更新
```

### 方式3：使用命令行启动本地服务器
```bash
cd dsa-review

# Python 用户：
python -m http.server 8000

# 或 Node.js 用户：
npx http-server -p 8000

# 然后在浏览器访问：http://localhost:8000
```

> 代码评判（在线编译）需要联网；其余 8 个模块全部离线可用。

---

## 第二步：浏览功能

网站包含 **9 个主要模块**，通过左侧导航栏切换：

### 📖 1. 知识检索
**用途**：查阅课程知识点、搜索概念

**操作**：
- 点击左侧章节树展开/收起，点击小节查看详细讲解
- 每节包含：定义 → 性质/过程 → C++ 代码 → 复杂度徽章 → 相关知识点链接
- 底部有「上一节 / 下一节」导航；**每章最后一节有「进入本章测试」按钮，直接开考**
- 使用顶部搜索框（或 `Ctrl + K`）全文检索

**试试**：
- 打开「7.5 平衡二叉树(AVL)」看四种旋转的讲解
- 搜索 "快速排序" 或 "KMP"
- 点章节名（如「第7章 查找」）看本章知识清单

---

### 🎓 2. 交互教学
**用途**：跟随分步骤课程深入学习（19 门课 / 127 步）

**操作**：
- 左侧按章节分组选择课程，顶部搜索框可过滤
- 「下一步/上一步」或键盘 `←`/`→` 翻页
- **步内小测**：答对才能解锁下一步（数字键 1-4 选项、Enter 确认）；可跳过但计入"未掌握"
- 代码走读步：当前行自动高亮 + 底部注释面板，点击带 ● 标记的行看解释
- 数组状态条：排序、堆、散列等过程的彩色方块演示
- 进度自动保存，下次进入恢复到上次位置；结课页有小结、未掌握步骤回看、测验/可视化跳转

**推荐课程**：
- 堆：筛选建堆与插入删除（9 步）
- AVL 树：四种旋转图解（8 步）
- KMP 算法：next 数组手推（7 步）
- 动态规划：0-1 背包与 LCS 填表（8 步）

---

### 📈 3. 算法可视化
**用途**：通过动画直观理解算法（**36 种**，与「交互教学」的课程一一呼应）

**操作**：
1. 选择算法（按组分：排序 / 线性表 / 栈与递归 / 树与堆 / 图 / 查找 / 矩阵与串 / 动态规划）
2. 「数据规模」滑杆控制数据量（**每类算法有独立量程与含义**，切换算法时滑杆自动换档，标签显示实际单位，如"6 盘子""10 顶点"；拖动滑杆数据规模真实变化）；「随机生成」换数据；「自定义」输入自己的数据
3. ▶ 播放 / ⏮⏭ 单步 / 🔄 重置 / 速度 0.25x~3x
4. 底部描述区同步解说每一步；比较/交换次数实时统计

**全部算法清单**：
| 分组 | 算法 |
|------|------|
| 排序 | 冒泡 / 选择 / 插入 / 归并 / 快速 / 堆排序 |
| 线性表 | 链表插入·删除·反转、顺序表插入·删除（元素搬移）、循环队列（判空/判满/回绕） |
| 栈与递归 | 中缀→后缀表达式（运算符栈逐步）、汉诺塔（递归move序列） |
| 树与堆 | BST 插入·删除、**先序/中序/后序遍历（非递归，展示栈状态）**、筛选法建堆、堆插入（上滤）、哈夫曼树构造、AVL 插入与四种旋转（含 bf 标注） |
| 图 | BFS / DFS / Dijkstra / 拓扑排序（入度表逐步删边） |
| 查找 | 线性 / 二分 / 散列线性探测（H(key)+探测次数）/ B 树插入与分裂（3 阶） |
| 矩阵与串 | KMP（next 递推+匹配）、稀疏矩阵快速转置、**正交链表（十字链表，行/列双向箭头）** |
| 动态规划 | LCS 填表、0-1 背包填表（逐格递推+方向） |

**自定义数据格式**：
| 算法类别 | 格式 | 非法输入处理 |
|---------|------|------------|
| 排序/链表/顺序表/树/堆/AVL/B树/哈夫曼/散列 | 逗号分隔整数（0~9999，各类别有上限） | 整条拒绝并指明坏 token/负数/超限 |
| 图（BFS/DFS/Dijkstra） | 顶点数; 每条边 u v 权值（顶点 1~n） | 顶点越界/自环/重复边/权重非法均拒绝 |
| KMP | 主串,模式串（主串≤30，模式串≤12） | 缺串/超长拒绝并提示 |
| 中缀→后缀 | 表达式（单字母/数字操作数，≤24 字符） | 非法字符/括号不配对拒绝 |
| 汉诺塔 | 盘数 3~6 | 越界拒绝 |
| LCS | X,Y 两串（各 ≤8 字符） | 缺串/超长拒绝 |
| 0-1 背包 | 容量 W; 每件物品 w v（≤5 件） | 分组/范围错误拒绝 |
| 稀疏转置/正交链表 | 行,列,值，分号分隔（≤16 个非零元） | 分组不对/负下标/值为 0/重复均拒绝 |
| 循环队列 | 暂不支持自定义（固定演示序列，覆盖判满/判空/回绕），滑杆可调容量 3~8 | 点击给出说明提示 |
| 拓扑排序 | 暂不支持自定义（随机生成并保证无环），滑杆可调顶点数 4~8 | 点击给出说明提示 |

---

### ⚙️ 9. 设置与存档
**用途**：自动存档、多份手动存档、启动恢复与数据管理

**操作**：
- **自动存档**：学习数据（教学进度、错题本、测验历史、收藏、代码草稿、对话记录等）变化后自动生成快照，另有定时兜底与关页前保存；若数据被误清，打开页面时会主动询问是否恢复
- **打开时恢复**：四项独立开关——上次浏览位置 / 代码评判草稿 / AI 对话记录 / 知识检索位置
- **手动存档**（最多 10 份）：完成一阶段复习后存一份，支持恢复（恢复前自动保留当前状态）、覆盖更新、重命名、下载 / 导入 JSON
- **危险区**：一键清空全部数据（建议先保存/下载一份存档）

**必试算法**：
- 快速排序（观察 partition 挖坑过程）
- 后序遍历（观察 tag 标记栈的第二次入栈）
- AVL 插入与旋转（观察 bf 何时变 ±2、LL/LR/RR/RL 如何选）
- B 树插入与分裂（观察中间关键字上浮、所有叶子永远同层）
- 正交链表（观察同一结点同时挂在行链表和列链表上）
- KMP（观察失配时按 next 右移、主串指针不回溯）

---

### 💻 4. 代码评判
**用途**：编写 C++ 代码，自动跑测试用例评分（21 道题）

**操作**：
1. 选择题目 → 题目模板自动加载
2. 补全你的实现
3. 需要参考代码结构时：点 **「📚 模板库」** 按钮（快捷键 `Ctrl + T`），弹窗内集中了 55+ 个模板——起步骨架（可直接运行的 main）、完整类模板（顺序表/单链表/排序/BST/图/DP）、知识库全部 46 个数据结构代码（顺序栈、循环队列、KMP、稀疏矩阵、哈夫曼、AVL、散列、并查集、八大排序……）。支持分类筛选、关键字搜索、代码预览，「✓ 使用此模板」直接载入编辑器，「📋 复制」复制代码，「📖 查看讲解」跳转对应知识点
4. 「▶ 运行」用左侧 stdin 试跑；「✓ 提交评测」跑全部测试用例（**各用例并行请求**，总耗时≈最慢的一个）
5. 查看逐用例结果（通过/答案错误/编译错误/运行时错误）；时间显示区分**程序真实执行时间**与含网络传输的总耗时

**注意**：换题或使用模板会直接替换编辑器内容——你写的代码已自动存为草稿（重开页面自动恢复），随时可在「设置与存档」找回；多数类/算法模板不含 `main()`，直接「运行」会链接失败，可搭配「起步骨架」分类使用。

**快捷键**：`Ctrl + Enter` 运行；`Ctrl + Shift + Enter` 提交评测

**题目覆盖**：线性表、栈与队列、串、数组与矩阵、树（BST/堆/哈夫曼/还原二叉树）、图（BFS/Dijkstra/拓扑/并查集）、排序、查找、动态规划——难度 easy×6 / medium×9 / hard×6

**关于在线编译**：双引擎自动回退（emkc Piston → Godbolt），都不可达时会给出明确提示；需要联网。

---

### 📝 5. 练习测验
**用途**：检验学习成果（219 道题，每章 ≥15 道）

**操作**：
1. 每章一张卡片：选难度 + 题量 → 开始；也可用顶部「快速跳转」链接直达章节
2. 动态计时：每题 45 秒，至少 5 分钟，时间到自动提交
3. 结果页：得分 + 难度维度小统计 + 「错题回顾 / 全部题目回顾」双标签
4. 答错的题**自动进入错题本**

---

### 📕 6. 错题本
**用途**：集中管理答错的题

**操作**：
- 按章节/难度筛选错题
- 「✏️ 重做」：页内作答，答对自动移出错题本
- 「🔁 重练筛选结果」：把筛选出的错题组成一套测验（做完后答对的自动移出）
- 「📥 导入」：粘贴 JSON（支持题目对象数组或题库 id 数组）
- 「📤 导出」：下载 JSON 文件，可再导入

---

### 🤖 7. AI 助教
**用途**：随时提问，获得课程知识解答

**操作**：输入框提问，点击「发送」或按 Enter；回答带相关话题芯片，点击芯片继续追问

**试试这些问题**：
- "什么是时间复杂度？"
- "AVL 什么时候旋转？"
- "散列冲突怎么处理？"

**注意**：当前使用规则引擎（37 条课程知识规则）模拟，未命中时会给热门话题兜底；可替换为真实 LLM API（改 `js/modules/aiChat/ruleEngine.js`）。

---

### 🔗 8. 拓展资源
**用途**：发现更多学习资源

浏览分类（可视化、练习、视频、书籍等），点击 ⭐ 收藏。「VisuAlgo」「LeetCode」「MIT 6.006」值得一试。

---

## 第三步：个性化设置

### 🌙 主题切换
点击侧边栏底部 🌙/☀️ 按钮切换亮色/暗色主题

### 💾 自动保存（localStorage）
| 数据 | 保存位置（localStorage 键） |
|------|---------------------------|
| 主题偏好 | `dsa-theme` |
| 教学课程进度 | `dsa-lesson-progress-v1` |
| 测验历史 | `dsa-quiz-history` |
| 错题本 + 导入的自定义题 | `dsa-mistake-book` / `dsa-mistake-custom-questions` |
| 收藏的资源 | `dsa-bookmarks` |
| 上次浏览位置 / 知识点 | `dsa-last-route` / `dsa-knowledge-pos` |
| 代码评判草稿 | `dsa-judge-state` |
| AI 对话记录 | `dsa-chat-history` |
| 自动存档快照 / 手动存档 | `dsa-archive-auto` / `dsa-archive-slots` |
| 设置板块偏好 | `dsa-settings` |

### 💾 存档与恢复（设置与存档板块）
- 打开页面自动回到上次位置（板块、知识点、代码草稿、对话，均可在设置中单独关闭）
- 数据变化后自动生成快照；若学习数据被误清，重开页面会询问是否从快照恢复
- 手动存档可保存多份命名存档，支持导出 JSON 文件备份 / 换浏览器导入

### 🔄 重置进度
- **全部重置**：「设置与存档」→ 危险区 → 清空全部数据（建议先保存存档）
- **只清教学进度**：`localStorage.removeItem('dsa-lesson-progress-v1')` 后刷新
- **只清错题本**：错题本页面「🗑 清空」按钮

---

## 第四步：推荐学习路径

### 📅 日常复习（每天 30 分钟）
1. **知识检索**（10 分钟）：复习 1-2 个知识点 + 代码
2. **算法可视化**（10 分钟）：单步跟踪 1 个算法
3. **练习测验**（10 分钟）：5 道题 + 错题解析

### 📅 周末深入学习（2 小时）
1. **交互教学**（30 分钟）：完成 1 门完整课程（含步内小测）
2. **代码评判**（60 分钟）：2-3 道编程题
3. **错题本 + AI 助教**（30 分钟）：重做错题、针对难点提问

---

## 常见问题

### Q: 进度保存在哪？如何重置？
**A**: 全部保存在浏览器 localStorage（不上传服务器）。重置方法见上方「重置进度」。换浏览器/电脑进度不互通——可先在「设置与存档」导出存档 JSON，再导入到新浏览器。

### Q: 打开页面弹出"是否从自动存档恢复"？
**A**: 说明检测到历史存档但当前学习数据为空（可能被误清）。点「确定」恢复数据；点「取消」从空白开始（存档仍保留，本会话内不会再询问）。

### Q: 代码无法运行？
**A**:
1. 确认联网（在线编译需要访问 emkc.org 或 godbolt.org）
2. 若提示 429（限流），稍等 1 分钟再试
3. 按 F12 看控制台错误；或把代码复制到 OnlineGDB 手动运行

### Q: 搜索结果不准确？
**A**: 换更精确的关键词；中英文都可以（"quick sort" 或 "快速排序"）。

### Q: AI 回答不符合预期？
**A**: AI 基于规则引擎，知识库有限；换个问法或查看「拓展资源」。

### Q: 可视化的"自定义"输入报错？
**A**: 按弹窗里的格式说明输入；报错信息会指出具体是哪个数字/token 非法。

---

## 下一步

1. ✅ 按推荐学习路径开始复习
2. 📖 阅读 `README.md` 了解技术细节与扩展方法
3. 🔧 改 `data/` 下的文件自定义内容（改完跑 `tools/` 校验）
4. 🐛 发现问题？查控制台 + `BUG_FIXES.md`

---

**祝你学习愉快，考试顺利！🎓✨**
